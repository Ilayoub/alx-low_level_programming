#include "main.h"

/**
 * _abs - the function computes the absolute value of an integer
 * @n: the integer that must be checked
 * Return: the absolute value of integer
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}

